<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationLayer"
        xmlns:ViewModel="clr-namespace:PresentationLayer.ViewModel" x:Class="PresentationLayer.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <ViewModel:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <DataGrid HorizontalAlignment="Left" Height="204" VerticalAlignment="Top" Width="201" Margin="27,67,0,0" ItemsSource="{Binding People}"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="90,289,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</Window>
